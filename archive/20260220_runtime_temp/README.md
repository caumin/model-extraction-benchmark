# 20260220_runtime_temp

Archived runtime-ephemeral directories moved from repository root:

- `temp/`
- `temp_configs/`

Reason:
- These folders are generated/temporary experiment artifacts.
- Keeping them in root increases clutter and risks accidental publishing.
- They can be recreated on demand by scripts or manual runs.
