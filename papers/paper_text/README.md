# paper_text

Canonical location for locally stored paper text extracts.

Naming rules:

- Use lowercase slugs with underscores (example: `dfme.txt`).
- For non-identical alternatives of the same paper, use `__variant_<source>.txt`.
- Exact duplicates should not remain here; move them to `archive/papers_duplicates/`.
