run:
  name: repro_2020_pal_activethief_cifar10_assumption_smoke_seed0
  seeds: [0]
  device: cuda:0

benchmark:
  protocol_version: "1.2"

victim:
  victim_id: cifar10_activethief_cnn
  arch: activethief_cnn
  channels: 3
  num_classes: 10
  input_size: [32, 32]
  checkpoint_ref: runs/victims/cifar10_activethief_cnn_seed0.pt
  output_mode: hard_top1
  temperature: 1.0

dataset:
  name: CIFAR10
  data_mode: surrogate
  surrogate_name: CIFAR10
  surrogate_resize: [32, 32]
  surrogate_max_samples: 10000
  surrogate_subset_seed: 42
  num_workers: 0
  channels: 3
  input_size: [32, 32]
  train_split: true

attack:
  name: activethief
  output_mode: hard_top1
  max_budget: 2000
  rounds: 10
  strategy: uncertainty
  batch_size: 150
  initial_seed_ratio: 0.1
  validation_budget_ratio: 0.2
  num_workers: 0
  pool_num_workers: 0
  train_num_workers: 0

substitute:
  arch: activethief_cnn
  dropout_prob: 0.2
  init_seed: 1234
  batch_size: 150
  num_workers: 0
  train_num_workers: 0
  val_num_workers: 0
  optimizer:
    name: adam
    lr: 0.001
    weight_decay: 0.001
    betas: [0.9, 0.999]
  max_epochs: 1000
  patience: 100

budget:
  max_budget: 2000
  checkpoints: [200, 400, 600, 800, 1000, 1200, 1400, 1600, 1800, 2000]

cache:
  enabled: true
  policy: temporary
  delete_on_finish: true
